# Fastlane Environment Variables Template
# Copy this file to .env and fill in your actual values
# Then run: gh secret set -f .env to push to GitHub repository secrets

# =============================================================================
# APPLE DEVELOPER ACCOUNT
# =============================================================================
# Your Apple Developer account email address
APPLE_ID=your-apple-id@example.com

# Your Development Team ID (found in Apple Developer Portal)
# Format: 10-character alphanumeric string (e.g., ABC1234567)
DEVELOPMENT_TEAM=YOUR_DEVELOPMENT_TEAM_ID

# Your App Store Connect Team ID (may be different from Development Team ID)
# Found in App Store Connect > Users and Access > Keys
APPSTORE_TEAM_ID=YOUR_APPSTORE_TEAM_ID

# =============================================================================
# APP STORE CONNECT API AUTHENTICATION (Recommended)
# =============================================================================
# Option 1: API Key (Recommended - more secure than session cookie)
# Create at: App Store Connect > Users and Access > Keys > App Store Connect API

# The Key ID of your App Store Connect API key
APP_STORE_CONNECT_API_KEY_KEY_ID=your_api_key_id

# The Issuer ID from your App Store Connect API key
APP_STORE_CONNECT_API_KEY_ISSUER_ID=your_issuer_id

# The content of your private key file (.p8)
# Replace newlines with \n in the actual key content
APP_STORE_CONNECT_API_KEY_CONTENT="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT_HERE\n-----END PRIVATE KEY-----"

# Option 2: Session Cookie (Alternative - expires regularly)
# Generate using: fastlane spaceauth -u your-apple-id@example.com
# FASTLANE_SESSION=your_fastlane_session_cookie

# =============================================================================
# CODE SIGNING (Fastlane Match)
# =============================================================================
# Password for encrypting/decrypting certificates in match repository
# Use a strong, unique password and store it securely
MATCH_PASSWORD=your_match_repository_password

# Git repository URL for storing encrypted certificates
# Should be a private repository (GitHub, GitLab, etc.)
MATCH_GIT_URL=https://github.com/your-org/certificates-repo.git

# Git authentication token for accessing the match repository
# Generate a Personal Access Token with repo access
MATCH_GIT_BASIC_AUTHORIZATION=your_git_basic_auth_token

# Optional: Specify match repository branch (default: master)
# MATCH_GIT_BRANCH=main

# Optional: Specify keychain name for match (default: login)
# MATCH_KEYCHAIN_NAME=fastlane_tmp_keychain

# =============================================================================
# CI/CD ENVIRONMENT
# =============================================================================
# Password for creating temporary keychain in CI environment
# Use a strong, random password for security
KEYCHAIN_PASSWORD=your_ci_keychain_password

# =============================================================================
# FASTLANE CONFIGURATION
# =============================================================================
# Reduce prompts and noise during fastlane execution
FASTLANE_SKIP_UPDATE_CHECK=1
FASTLANE_HIDE_GITHUB_ISSUES=1
FASTLANE_HIDE_CHANGELOG=1

# Optional: Disable fastlane analytics
# FASTLANE_OPT_OUT_USAGE=1

# Optional: Set custom temporary directory
# FASTLANE_TMPDIR=/tmp/fastlane

# =============================================================================
# OPTIONAL: ADDITIONAL CONFIGURATION
# =============================================================================
# Slack webhook URL for notifications (optional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Discord webhook URL for notifications (optional)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR/DISCORD/WEBHOOK

# Custom app identifier (if different from Xcode project)
# APP_IDENTIFIER=com.yourcompany.yourapp

# Custom app scheme (if different from default)
# APP_SCHEME=YourAppScheme

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. NEVER commit the actual .env file to version control
# 2. Use strong, unique passwords for all services
# 3. Rotate API keys and certificates regularly
# 4. Follow principle of least privilege for service accounts
# 5. Use GitHub repository secrets for CI/CD environments
# 6. Consider using a secrets management service for production